todos
- fix the weird loading issue with page load and scrolling
- what looks weird is that the notes on like left most column and middle usually load already while the right most column is still loading, and once it loads the page jumps a bit where like certain notes get repositioned